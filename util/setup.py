from setuptools import setup, find_packages

setup(
    name="utility",
    version="0.0.1",
    packages=find_packages(),
    author="raccoon_zero",
    description="Includes temporal_utils"
)

print(find_packages())